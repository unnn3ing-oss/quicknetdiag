# 家用網路醫生（netdiag）

自動偵測「連線卡頓、頻繁斷線、WiFi 連線問題」的成因，幫你分清楚問題到底是出在：

- **WiFi 訊號／路由器**（區網本身不穩）
- **ISP／數據機**（區網正常但對外連不出去）
- **DNS 伺服器**（能連上 IP 但網域解析不到或很慢）

單一 Python 檔案，不需要額外安裝套件，Mac / Windows / Linux 都能跑。

## 需求

- Python 3.8 以上（Mac / Linux 通常內建；Windows 到 [python.org](https://www.python.org/) 下載安裝）
- 系統本身要有 `ping` 指令（三大平台都內建）

## 使用方式

### 單次快篩

```bash
python3 netdiag.py
```

會自動偵測路由器 IP，測試：到路由器的延遲/遺失率、對外連線（Cloudflare／Google DNS）、
幾個常見網域的 DNS 解析時間、WiFi 訊號強度，然後印出診斷結果與建議。

### 長時間監控（抓間歇性斷線最好用）

間歇性斷線常常「當下想測就沒事，你不注意的時候才斷」，所以真正有用的是背景長時間監控：

```bash
# 監控 1 小時，每 5 秒量測一次（預設間隔）
python3 netdiag.py --monitor 3600

# 持續監控直到手動按 Ctrl+C
python3 netdiag.py --monitor 0

# 監控並把每筆量測記錄到 CSV，方便之後用 Excel 畫圖看趨勢
python3 netdiag.py --monitor 0 --csv wifi_log.csv --interval 3
```

監控結束（時間到或 Ctrl+C）時會印出總結：斷線次數、最長斷線時間、平均延遲/抖動、
WiFi 訊號範圍，以及根據數據做的整合判斷（例如「區網穩定但對外遺失率偏高 → 問題在 ISP」）。

建議做法：把它丟到背景跑一整天（尤其是常常卡頓的時段），隔天回來看 CSV 或總結，
就能知道斷線是不是集中在特定時間（例如晚上尖峰、鄰居下班用網高峰）。

```bash
# 背景執行範例（Mac/Linux）
nohup python3 netdiag.py --monitor 0 --csv wifi_log.csv > netdiag.log 2>&1 &
```

### 其他參數

| 參數 | 說明 |
|---|---|
| `--gateway IP` | 手動指定路由器 IP（預設自動偵測，抓不到時用這個） |
| `--target NAME=HOST` | 額外加測的目標，例如 `--target 公司VPN=10.0.0.1`，可重複使用 |
| `--count N` | 單次快篩時每個目標的 ping 次數，預設 8 |
| `--no-color` | 關閉終端機彩色輸出 |

## 判斷邏輯（簡化版）

| 現象 | 判斷 | 建議方向 |
|---|---|---|
| 路由器完全無回應 | 區網／裝置本身斷線 | 檢查路由器/AP 電源、Mesh 節點回程 |
| 路由器有回應但遺失率高 | WiFi 訊號或干擾問題 | 靠近 AP、換頻道、排除微波爐/藍牙干擾 |
| 路由器延遲/抖動偏高 | 2.4GHz 壅塞或 Mesh 回程差 | 切 5GHz/6GHz，或改有線回程 |
| 區網正常但對外連線異常 | ISP／數據機問題 | 重開數據機、回報電信業者 |
| 能連 IP 但網域解析失敗 | DNS 伺服器問題 | 改用 1.1.1.1 或 8.8.8.8 |
| WiFi 訊號弱（<-70dBm 或 <40%） | 距離/阻隔造成訊號弱 | 靠近 AP、增設 Mesh 節點 |

實際輸出會依當下量到的數字給出更明確的建議。

## 即時儀表板（網頁版，可存成 Dock App）

除了終端機模式，還有一個網頁儀表板：背景持續監控，網頁分兩頁（頂部頁籤切換）——
**「網路架構」**（首頁）是可以拖曳編輯的設備拓樸圖，**「即時監控」**是延遲/遺失率/頻寬
趨勢圖與診斷結果，兩頁共用最上面那排即時數值卡。淺色介面、字級加大方便閱讀。右上角
狀態燈旁邊有「上次更新時間」跟「立即更新」按鈕，不用等輪詢。

```bash
python3 dashboard/server.py
```

啟動後瀏覽器開 <http://127.0.0.1:8765>。全程本機執行，資料不會送到任何外部伺服器。

**存成 Mac Dock App（macOS Sonoma 以上）**：Safari 開啟該網址 → 選單「檔案」→
「加入 Dock」，就會變成一個獨立視窗的 App 圖示，跟開網頁分開，關掉終端機前它會持續更新
（只要 `dashboard/server.py` 還在背景跑）。

**設定你家的網路架構圖**：直接在網頁上編輯即可，不用手動改 JSON。

- **拖曳卡片**調整位置：放開後會自動避開其他卡片（固定間距、絕不重疊），拖到別的樓層
  區塊範圍內，那台設備的樓層也會自動改過去。
- **點擊卡片**（不是拖曳）會跳出編輯視窗：設備編號、名稱、IP、MAC、位置、樓層、設備類型、
  上層設備、連線類型全部都能改，改完按「儲存」直接寫回設定檔。
- **「＋ 新增設備」**：在架構圖面板右上角，新增一台設備並指定它接在哪台上層設備底下。
- **編輯視窗裡的「刪除設備」**：刪除後，它底下的子設備會自動過繼給它的上層設備，不會斷鏈。

所有異動都是**直接覆寫**你現在用的那個設定檔（`--topology-file` 指到哪個檔案就存哪個），
不會另外存一份。如果那個檔案原本是另一套掃描工具（例如 `network-monitor`）產生的
`{"devices": {...}, "topology": {...}}` 格式，儀表板只會動它管理的欄位（名稱、IP、MAC、
位置、樓層、類型、座標、連線關係），品牌／備註／VLAN 之類其他工具自己寫入的欄位不會被動到：

```bash
python3 dashboard/server.py --topology-file /path/to/devices.json
```

初次沒有任何設定檔時，架構圖是空的，用「＋ 新增設備」一台一台建立即可；也可以先照
`dashboard/topology.json` 的格式手動寫幾筆當起點（裡面附中文說明），存檔後重新整理網頁
就會套用。設備的線上/離線狀態一律由這個儀表板自己即時 ping，不依賴來源檔案裡舊的
`is_online` 欄位。

架構圖圖例：藍線＝有線、橘線＝PoE、虛線＝WiFi；綠燈＝線上、紅燈＝離線、灰燈＝無管理 IP
（純交換器等結構節點）。「未分類區」放的是抓得到但沒手動分類樓層的設備（手機、筆電等）。

**排版方式**：卡片一律對齊固定間距的虛擬格線（同一樓層裡，上層設備在上面那一列、
子設備在下面那一列，同一列左右排開），連線一律是直線＋直角，不會有兩張卡片重疊、
也不會有線沒接到卡片的情況。第一次讀到某個檔案（不管是你自己寫的還是另一套掃描
工具匯出的）時會整份重新排一次乾淨格線，跟原本那套工具畫面上的實際位置不會一樣；
排過一次之後就會記住（存檔會在檔案裡加一個 `_netdiag_layout_v2` 標記），之後只有
新增的設備需要補位置，不會每次讀檔都重排、把你拖曳調整過的順序打亂。

**背景長期執行**：

```bash
nohup python3 dashboard/server.py --csv wifi_log.csv > dashboard.log 2>&1 &
```

**常用參數**：

| 參數 | 說明 |
|---|---|
| `--port 8765` | 網頁伺服器埠號 |
| `--host 0.0.0.0` | 開放家用網路內其他裝置（例如手機）連進來看；預設只有這台電腦能看 |
| `--interval 5` | 量測間隔秒數 |
| `--history 720` | 圖表保留的資料點數（預設約 1 小時） |
| `--gateway IP` | 手動指定路由器 IP |
| `--csv FILE` | 同時把每輪量測記錄到 CSV |
| `--topology-file PATH` | 自訂拓樸圖設定檔路徑 |

## 已知限制

- WiFi 訊號讀取依平台而異，且部分系統（例如新版 macOS 拿掉了 `airport` 指令、或用有線網路）
  可能讀不到，這種情況下腳本仍會正常執行其他測試，只是不會顯示 WiFi 訊號。
- 「即時頻寬」量測的是這台電腦對外那張網卡的累積流量差值（Mac/Linux 抓得到實際介面名稱；
  Windows 目前退而求其次用全介面加總，不是只算對外那張網卡，數字僅供參考）。量的是
  「這台電腦」的用量，不是「全家」的用量；如果只是想看有沒有掉速，仍具參考價值。
- 需要系統本身有 `ping` 指令的執行權限；企業/防火牆環境可能封鎖 ICMP，導致誤判為斷線，
  這種情況可用 `--target` 換成你確定會回應 ping 的內部主機測試。
- 這是本機診斷工具，測的是「你這台電腦到路由器/外部的連線品質」，不是路由器本身的系統紀錄；
  如果路由器有自己的日誌功能（多數 Mesh 系統 App 都有），可以互相對照。
- 如果 `--topology-file` 指向的是另一套工具（例如 `network-monitor`）**還在背景執行、也會
  自己寫這個檔案**的那份，兩邊同時寫檔理論上有極小機率互相覆蓋對方剛存的東西；平常操作
  （拖曳/新增/編輯間隔幾秒）不會有事，只是不建議兩邊「同一瞬間」都在狂寫。
